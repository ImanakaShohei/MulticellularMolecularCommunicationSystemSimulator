# PeformanceType 列挙型
実行時のパフォーマンス設定を表します。
速度を優先するか、メモリ使用量効率を優先するかを選択します。

```c++
enum class PeformanceType;
```

## メンバー
| 名前 | 値 | 説明 |
| :--- | :--- | :--- |
| `Fast` | `0` | 速度を優先します。 |
| `LowMemory` | `1` | メモリ効率を優先します。 |